/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
            hostname:"15.235.192.200",
            }
        ]
    }
};

export default nextConfig;
