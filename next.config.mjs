/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
            hostname:"153.92.4.56",
            }
        ]
    }
};

export default nextConfig;
